const map=L.map('map').setView([-7.2,-35.8],7);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap'}).addTo(map);let markers=[];const sellers=window.SELLERS||[];function render(state='ALL'){markers.forEach(m=>map.removeLayer(m));markers=[];sellers.filter(s=>state==='ALL'||s.state===state).forEach(s=>{if(!s.lat||!s.lng)return;let m=L.marker([s.lat,s.lng]).addTo(map).bindPopup(`<b>${s.name}</b><br>${s.role}<br>${s.city}/${s.state}<br><a href="https://wa.me/${s.phone.replace(/\D/g,'')}">${s.phone}</a>`);markers.push(m)});}function filterMap(s){render(s)}render();
